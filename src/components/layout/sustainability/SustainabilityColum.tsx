import { PropsWithChildren } from 'react'

export const SustainabilityColum = ({ children }: PropsWithChildren) => {
	return (
		<div className='relative px-5 min-h-[783px] pl-7 py-20'>{children}</div>
	)
}
